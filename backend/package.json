{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "./dist/src/server.js",
  "scripts": {
    "build:clean": "rm -rf dist",
    "build": "npm run build:clean && tsc",
    "build:watch": "npm run build:clean && tsc -w",
    "start": "NODE_ENV=development node dist/server.js",
    "start:dev": "npm run migrate:dev && NODE_ENV=development tsc-watch --onSuccess 'npm run start'",
    "migrate:dev": "prisma migrate dev"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^17.0.10",
    "ts-node-dev": "^1.1.8",
    "tsc-watch": "^4.6.0",
    "typescript": "^4.5.4"
  },
  "dependencies": {
    "@prisma/client": "^3.8.1",
    "dotenv": "^14.2.0",
    "fastify": "^3.27.0",
    "fastify-plugin": "^3.0.0",
    "prisma": "^3.8.1",
    "purify-ts": "^1.1.0",
    "ts-node": "^10.4.0"
  }
}
